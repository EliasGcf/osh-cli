import { Router } from 'express';

const {{ routeName }}Router = Router();

// {{ routeName }}.post('/', async (req, res) => res.send('POST - {{ routeName }}'));

// {{ routeName }}.get('/', async (req, res) => res.send('GET - {{ routeName }}'));

// {{ routeName }}.get('/:id', async (req, res) => res.send('GET ONE - {{ routeName }}'));

// {{ routeName }}.put('/', async (req, res) => res.send('PUT - {{ routeName }}'));

// {{ routeName }}.delete('/', async (req, res) => res.send('DELETE - {{ routeName }}'));

export default {{ routeName }}Router;
